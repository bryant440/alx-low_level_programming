#include "main.h"

/**
 * mul - Checks for a digit (0-9).
 * @a: First number to be multiplied.
 * @b: Second number to be multiplied.
 *
 * Return: 1 if the number is a digit, 0 otherwise.
 */
int mul(int a, int b)
{
return (a * b);
}
